# Java Class Constant Pool Printer

Prints the constant pool of a Java class file. Currently prints all values except Dynamic, InvokeDynamic, and MethodHandle.

Compile: `make`

Run: `./dump_constant_pool YourClass.class`
